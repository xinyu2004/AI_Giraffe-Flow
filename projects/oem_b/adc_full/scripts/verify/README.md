# Verify — oem_b / adc_full

本演示 SKU 的验收脚本（与工程同树）。

| Script | Purpose |
|--------|---------|
| [smoke_mcu_desktop.sh](smoke_mcu_desktop.sh) | MCU desktop IPC（peer ↔ gateway，无真 MCU） |

默认构建树：`projects/oem_b/adc_full/build-mcu-desktop/`（可用 `GF_BUILD_DIR` 覆盖）。
