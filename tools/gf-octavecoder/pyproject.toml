[project]
name = "gf-octavecoder"
version = "0.1.0"
description = "Domain-scoped Octave→C++ for Giraffe planning (not a general Octave coder)"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0"]

[project.scripts]
gf-octavecoder = "gf_octavecoder.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
