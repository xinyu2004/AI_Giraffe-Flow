# cross_domain IPC binding

AP ↔ peer domain (e.g. MCU AUTOSAR CP) when `topology: ap_mcu_cp`.

Implemented by `mcu.cp_gateway` process + this binding plugin (later).

Parent: [bindings/README.md](../README.md)
