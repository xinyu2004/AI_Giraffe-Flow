# archive/

本目录曾放完整车身 DBC 归档。  
当前 demo 用的 `Demo_Car_*.dbc` 为随意样例，**已删除**。

正式 OEM 全量通信矩阵如何收、如何提炼成 `oem_import.dbc` + 模块 hpp，见后续讨论（将写入 `docs/zh/architecture/sor-authoring.md` / P0_PLAN）。

运行时导入只用：[`projects/oem_demo/vehicle_demo/oem/oem_import.dbc`](../../projects/oem_demo/vehicle_demo/oem/oem_import.dbc)
