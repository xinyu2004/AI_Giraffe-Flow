# bridge/

Optional bridges (not required for MVP core path).

| Dir | Intent |
|-----|--------|
| [ros2/](ros2/) | Helpers / examples for ROS 2 ↔ Giraffe Flow (prefer DDS direct first) |

Keep bridges separable so board production profiles do not pull desktop ROS.
