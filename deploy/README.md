# deploy/

| Path | Purpose |
|------|---------|
| [profiles/](profiles/) | `desktop` / `board` / `vehicle-debug` / `production` knobs |
| [manifests/](manifests/) | Example EM/PHM process lists (generated later from SOR) |

Profiles decide which bindings, apps, and debug agents are enabled — not #ifdef in business code.
