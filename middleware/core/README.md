# core

`gf_ara::core` — Result / ErrorCode (P0 静态平台代码).

```text
include/gf_ara/core/
  error_code.hpp
  result.hpp
```

Build (repo root):

```bash
cmake -B build -DGF_BUILD_TESTS=ON
cmake --build build --target gf_core_smoke
./build/middleware/core/gf_core_smoke
```

Parent: [middleware/README.md](../README.md)
