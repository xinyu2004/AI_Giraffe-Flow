# P0 实施计划（待展开）

> 路线图上下文：[ROADMAP.md](ROADMAP.md)

P0 目标：**SOR 0.2 子集冻结 + gf-codegen 最小管线 + iceoryx 双进程 demo + ARM OSAL**。

## 建议任务顺序

1. 冻结 SOR 0.2 必填字段评审
2. CMake 根工程 + `middleware/core` Result
3. `gf-codegen lint` + `generate` 最小模板
4. iceoryx binding spike → 两进程 demo
5. OSAL 单调时钟 + 线程
6. CI 冒烟

## 下一对话

在本文件基础上细化：人天、负责人、第一个 demo 日期、P0 不做的明确清单。
