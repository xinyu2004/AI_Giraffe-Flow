# uss_feed

P0 B3 simulator: publishes `semantic.uss_sensing / 1 / UssZones` over iceoryx.

Requires RouDi (`iox-roudi`). Prefer:

```bash
bash scripts/run_iox_demo.sh
```

Parent: [../README.md](../README.md)
