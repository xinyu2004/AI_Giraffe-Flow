# Placeholder GitHub Actions / CI config
# name: giraffe-flow-ci
# on: [push, pull_request]
# jobs:
#   docs:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v4
#       - run: echo "skeleton — enable real checks later"
